{
  "name": "api-d",
  "scripts": {
    "dev:no": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --minify --dts --outdir=dist",
    "start": "bun run src/index.ts",
    "start:prod": "bun run dist/index.js"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.2",
    "@repo/db": "workspace:*",
    "@repo/schema": "workspace:*",
    "drizzle-orm": "^0.38.4",
    "hono": "^4.6.17",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
